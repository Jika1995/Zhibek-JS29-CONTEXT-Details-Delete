import React from 'react'

const Button = () => { //props //{children}
    // console.log(children); //Register Product List
  return (
    <></>
    // <button>{props.text}</button>
    // <button>{children}</button>
    
  )
}

export default Button